# Task 001: Setup Inicial da Landing Page
**Status: ✅ Concluída**

## Detalhes
- **Tipo:** Setup
- **Camada:** Frontend (Next.js)

## Escopo
Criar o esqueleto base da página de lançamento em `src/app/obagavealancamento/page.tsx` para garantir que as metatags e a estrutura principal do React estejam prontas.

## Tarefas
1. Criar a pasta e o arquivo da rota (`/obagavealancamento`).
2. Configurar as tags SEO (`title: "OBA Gávea | Curso de Desenho no Shopping da Gávea"` e `description`).
3. Importar a estrutura básica de Header e Footer globais (se aplicável, ou criar um layout limpo específico para a Landing Page).
4. Preparar o componente `page.tsx` com as tags semânticas `<main>` onde os blocos serão injetados.
